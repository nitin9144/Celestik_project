[build]
  command = "npm install && npm run build"
  publish = "dist"     # change to "build" only if your vite.config.js outputs to build folder

[build.environment]
  NPM_CONFIG_PRODUCTION = "false"